{
    "name": ".parent",
    "versions": "All",
    "description": "Get the parents of the current elements, optionally matching the provided selector or element(s)",
    "signatures": [
        "$parents = zq.parent()",
        "$matchingParents = zq.parent( string|Element|zQuery|ArrayLike comparison )"
    ],
    "arguments": [
        {
            "name": "comparison",
            "description": "The selector or element(s) to compare descendants to. If it's an array-like or zQuery object, only one element in the object must match for it to count as a match."
        }
    ],
    "returns": [
        "A new zQuery instance containing parents"
    ]
}