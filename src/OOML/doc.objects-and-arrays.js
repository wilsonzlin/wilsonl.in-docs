{
    name: 'Objects and Arrays',
    description: 'Objects and Arrays',
    content:
        ['Only object literals count as the `Object` type in OOML. Object literals are, put simply, objects created with the braces syntax `{}`.',
        'OOML.Array instances do not count as the `Array` type in OOML, nor are they subclasses of the native `Array` class.'].join('\n\n'),
}
