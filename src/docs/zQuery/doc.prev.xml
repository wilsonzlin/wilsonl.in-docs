<entry>
	<name>.prev</name>
	<versions>All</versions>
	<description>Get the immediate element prior to each of the current elements, optionally matching the provided selector or element(s)</description>

	<signatures>
		<signature>$sibingBefore = zq.prev()</signature>
		<signature>$matchingSiblingBefore = zq.prev( string|Element|zQuery|ArrayLike comparison )</signature>
	</signatures>

	<arguments>
		<argument>
			<name>comparison</name>
			<description>The selector or element(s) to compare the sibling of each current element to. If it's an array-like or zQuery object, only one element in the object must match for it to count as a match.</description>
		</argument>
	</arguments>

	<returns>
		<return>A new zQuery instance containing the elements that are directly before the current elements</return>
	</returns>
</entry>