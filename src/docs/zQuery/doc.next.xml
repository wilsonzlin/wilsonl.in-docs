<entry>
	<name>.next</name>
	<versions>All</versions>
	<description>Get the immediate element following each of the current elements, optionally matching the provided selector or element(s)</description>

	<signatures>
		<signature>$sibingAfter = zq.next()</signature>
		<signature>$matchingSiblingAfter = zq.next( string|Element|zQuery|ArrayLike comparison )</signature>
	</signatures>

	<arguments>
		<argument>
			<name>comparison</name>
			<description>The selector or element(s) to compare the sibling of each current element to. If it's an array-like or zQuery object, only one element in the object must match for it to count as a match.</description>
		</argument>
	</arguments>

	<returns>
		<return>A new zQuery instance containing the elements that are directly after the current elements</return>
	</returns>
</entry>