<entry>
	<name>.closest</name>
	<versions>All</versions>
	<description>Get the current elements or their closest ancestors that match the provided selector or element(s)</description>

	<signatures>
		<signature>$matchingAncestors = zq.closest( string|Element|zQuery|ArrayLike comparison )</signature>
	</signatures>

	<arguments>
		<argument>
			<name>comparison</name>
			<description>The selector or element(s) to compare found elements to. If it's an array-like or zQuery object, only one element in the object must match for it to count as a match.</description>
		</argument>
	</arguments>

	<returns>
		<return>A new zQuery instance containing the matching elements</return>
	</returns>
</entry>