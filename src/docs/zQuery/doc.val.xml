<entry>
	<name>.val</name>
	<versions>All</versions>
	<description>Get the first or set every element's value</description>

	<signatures>
		<signature>zq = zq.val( newValue )</signature>
		<signature>value = zq.val()</signature>
	</signatures>

	<arguments>
		<argument>
			<name>newValue</name>
			<description>The value to set to each element that can have a value. `&lt;select&gt;` elements will not be set.</description>
		</argument>

		<argument>
			<name>(no argument)</name>
			<description>Return the value of the first element in the current instance.<zc-newline />
			- Will return `null` if the element is `&lt;select disabled&gt;`.<zc-newline />
			- Will return an array containing selected and enabled option values if the element is a `&lt;select multiple&gt;`.<zc-newline />
			- Will return the chosen file for an `&lt;input type="file"&gt;`.<zc-newline />
			- Will return an array containing chosen files of an `&lt;input type="file" multiple&gt;`.</description>
		</argument>
	</arguments>

	<returns>
		<return>The current zQuery instance</return>
		<return>An array containing or string formed from all elements' text content</return>
	</returns>
</entry>