<entry>
	<name>.parent</name>
	<versions>All</versions>
	<description>Get the parents of the current elements, optionally matching the provided selector or element(s)</description>

	<signatures>
		<signature>$parents = zq.parent()</signature>
		<signature>$matchingParents = zq.parent( string|Element|zQuery|ArrayLike comparison )</signature>
	</signatures>

	<arguments>
		<argument>
			<name>comparison</name>
			<description>The selector or element(s) to compare descendants to. If it's an array-like or zQuery object, only one element in the object must match for it to count as a match.</description>
		</argument>
	</arguments>

	<returns>
		<return>A new zQuery instance containing parents</return>
	</returns>
</entry>